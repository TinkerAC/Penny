package app.penny.presentation.enumerate

/**
 * AppTheme mode enum class,
 * STATIC use hard code MaterialTheme,
 * DYNAMIC use seed color to generate dynamic appTheme.
 */
enum class ThemeMode {
    STATIC,
    DYNAMIC
}